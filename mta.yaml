_schema-version: "3.1"
ID: SAP2
description: A simple CAP project.
version: 1.0.0
modules:
- name: SAP2-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: postgresql
  - name: SAP2-auth
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
  build-parameters:
    builder: npm
- name: SAP2
  type: approuter.nodejs
  path: app/
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: SAP2-auth
  - name: SAP2-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  parameters:
    buildpack: nodejs_buildpack
    disk-quota: 2048M
    keep-existing-routes: true
    memory: 256M
    stack: cflinuxfs4
- name: SAP2-postgresql-deployer
  type: nodejs
  path: gen/pg
  requires:
  - name: postgresql
  parameters:
    buildpack: nodejs_buildpack
    no-route: true
    no-start: true
    tasks:
    - command: npm start
      name: deploy-to-postgresql
- name: SAP2-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: SAP2-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comsatinfotechcloudappskitchen.zip
      name: comsatinfotechcloudappskitchen
      target-path: resources/
- name: comsatinfotechcloudappskitchen
  type: html5
  path: app/kitchen
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: postgresql
  type: org.cloudfoundry.managed-service
  parameters:
    service: postgresql-db
    service-plan: trial
- name: SAP2-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: SAP2-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: SAP2-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: SAP2-html5-srv
    service-plan: app-host
- name: SAP2-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: SAP2-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
